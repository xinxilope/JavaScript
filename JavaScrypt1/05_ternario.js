const minimo = 16;
const real = 18;

// if else

if (real >= minimo) {
    console.log("liberado")
} else {
    console.log("nao liberado")
}

// ou

console.log(real >= minimo ? "liberado" : "nao liberado")